<?xml version="1.0"?>
<!DOCTYPE suppressions PUBLIC
  "-//Checkstyle//DTD SuppressionFilter Configuration 1.2//EN"
  "https://checkstyle.org/dtds/suppressions_1_2.dtd">
<suppressions>
  <!-- Suppress Javadoc requirements on the Spring bootstrap class (infrastructure, not API). -->
  <suppress files="ServerApplication\.java" checks="SummaryJavadoc"/>
  <suppress files="ServerApplication\.java" checks="MissingJavadocType"/>
  <suppress files="ServerApplication\.java" checks="MissingJavadocMethod"/>
  <!-- "WhoAmI" is the intended, readable name for the /v1/me auth probe; the consecutive-capital
       rule would force an unnatural rename. Suppress the abbreviation check on those types only. -->
  <suppress files="WhoAmI.*\.java" checks="AbbreviationAsWordInName"/>
</suppressions>
